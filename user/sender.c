//sender program

// can uncomment the bottom part

// char *fname = "/path/to/file/filename"; int fd = syscall_open_named_pipe(fname);
// char buffer[] = "Hello World\n"; syscall_object_write(fd,buffer,strlen(buffer));